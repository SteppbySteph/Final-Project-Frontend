import React from 'react'

import { LoadingSpinner, LoadingSpinnerContainer } from './Styles'

const Loading = () => {
    return (
        <LoadingSpinnerContainer>
            <LoadingSpinner />
        </LoadingSpinnerContainer>
    )
}

export default Loading
